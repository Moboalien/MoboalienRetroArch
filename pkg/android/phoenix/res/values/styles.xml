<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Translucent floating activity used as a "screen being streamed" notice
         while the native activity window renders into the encoder surface. -->
    <style name="Theme.StreamingNotice" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
